<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
{{range .Sitemap}}
<url>
	<loc>{{.Loc}}</loc>
	{{with .LastMod}}<lastmod>{{.}}</lastmod>{{end}
	{{with .ChangeFreq}}<changefreq>{{.}}</changefreq>{{end}
	{{with .Priority}}<priority>{{.}}</priority>{{end}}
</url>
{{end}}
</urlset>
