<!DOCTYPE html>
<html>
{{template "header.tmpl" .}}
<body>
	<header>
	<p id="blog_title"><a href="/">{{.Config.Blog.Title}}</a></p>
	<p id="blog_subtitle">{{.Config.Blog.Subtitle}}</p>
	</header>
<div>
{{$nar := len .Articles.Enabled}}
{{range .Articles.Enabled}}
	<article>
		<header>
			<h1><a href="/{{.Slug}}">{{.Title}}</a></h1>
		</header>
		{{$comments := .Comments.Enabled}}
		{{if eq $nar 1}}
			{{.Body}}
			{{with $comments}}
			<article id="comments">
			<h4>Comments</h4>
				{{range .}}
				<article>
				<header>
				<p>On <time>{{.PostDate}}</time> {{.Name}} wrote in to say &hellip;</p>
				</header>
				<blockquote>{{.Comment}}</blockquote>
				</article>
				{{end}}
			</article>
			{{end}}
		{{else}}
			{{.ReadMore}}
			{{if .HasMore}}
				<p><a href="/{{.Slug}}">Read the rest of this story &hellip;</a></p>
			{{end}}

			<footer>
			<p>Published: <time>{{.PostDate}}</time></p>
			{{if $comments}}
			<p>Comments: <a href="/{{.Slug}}#comments">{{len $comments}}</a></p>
			{{end}}
			{{with .Tags}}
			<p>Tagged: {{range .}}<a href="/tag/{{.}}">{{.}}</a> {{end}}</p>
			{{end}}
			</footer>
		{{end}}
	</article>

{{end}}
</div>

<aside>
{{/*
	<section id="archive">
	<h3>Archive</h3>
	<ul>
	{{range .Archive}}
	<li><a href="/{{.Year}}/">{{.Year}}</a> ({{.Count}})</li>
	<ul>
	{{range .Month}}
	<li><a href="/{{.Year}}/{{.Month}}/">{{.Month}}</a> ({{.Count}})</li>	
	<ul>
	{{range .Slugs}}
	<li><a href="/{{.Slug}}">{{.Title}}</a></li>
	{{end}}
	</ul>
	{{end}}
	</ul>
	{{end}}
	</ul>
	</section>
 */}}

	<section id="tagcloud">
	<h3>Tag Cloud</h3>
	<ul>
	{{range .TagCloud}}
	<li class="tagcloud_{{.Wight}}"><a href="/tag/{{.Tag}}">{{.Tag}}</a></li>
	{{end}}
	</ul>
	</section>
</aside>

	{{with .Error}}
	<p>{{.}}</p>
	{{end}}

<footer>
	{{with .PrevPage}}
	<a href="?page={{.}}">&larr; Newer Articles</a>
	{{end}}

	{{with .NextPage}}
	<a href="?page={{.}}">Older Articles &rarr;</a>
	{{end}}
</footer>
</html>
